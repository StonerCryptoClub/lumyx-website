const GOOGLE_SHEETS_WEBHOOK = 'https: (function initVSL() { if (typeof VSL_YOUTUBE_URL === 'undefined' || !VSL_YOUTUBE_URL) return; const wrapper = document.getElementById('vsl-wrapper'); if (!wrapper) return; const placeholder = document.getElementById('vsl-placeholder'); if (placeholder) placeholder.remove(); const iframe = document.createElement('iframe'); const videoId = extractYouTubeId(VSL_YOUTUBE_URL); if (!videoId) return; iframe.src = `https: iframe.title = 'Lumyx Consulting Strategy Overview'; iframe.allow = 'accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture'; iframe.allowFullscreen = true; wrapper.appendChild(iframe); })(); function extractYouTubeId(url) { const match = url.match( /(?:youtu\.be\/|youtube\.com\/(?:watch\?v=|embed\/|v\/))([^?&"'>]+)/ ); return match ? match[1] : null; } document.addEventListener('DOMContentLoaded', function () { const headlineLine = document.querySelector('.hero-headline-line'); if (headlineLine) { requestAnimationFrame(() => { requestAnimationFrame(() => { headlineLine.classList.add('is-visible'); }); }); } const form = document.getElementById('hero-lead-form'); if (!form) return; const submitBtn = document.getElementById('hf-submit'); const errorBox = document.getElementById('hf-error'); form.addEventListener('submit', async function (e) { e.preventDefault(); const name = form.querySelector('#hf-name').value.trim(); const email = form.querySelector('#hf-email').value.trim(); const phone = form.querySelector('#hf-phone').value.trim(); const business = form.querySelector('#hf-business').value.trim(); const service = form.querySelector('#hf-service').value; if (!name || !email || !phone || !business || !service) { showError('Please fill in all fields before submitting.'); return; } if (!isValidEmail(email)) { showError('Please enter a valid email address.'); return; } setLoading(true); hideError(); const payload = { name, email, phone, business, service }; let submitSucceeded = false; const originalBtnText = submitBtn.querySelector('.hf-btn-text')?.textContent || 'Get My Free Strategy Call ->'; try { const timeoutMs = 12000; const timeoutPromise = new Promise((_, reject) => { setTimeout(() => reject(new Error('Request timeout')), timeoutMs); }); await Promise.race([ fetch(GOOGLE_SHEETS_WEBHOOK, { method: 'POST', mode: 'no-cors', body: JSON.stringify(payload), keepalive: true, }), timeoutPromise, ]); submitSucceeded = true; try { sessionStorage.setItem('lumyx_lead', JSON.stringify(payload)); } catch (_) {} const bookingSection = document.getElementById('booking-section'); if (bookingSection) { bookingSection.scrollIntoView({ behavior: 'smooth', block: 'start' }); } const btnText = submitBtn.querySelector('.hf-btn-text'); if (btnText) btnText.textContent = 'Done! Scroll down to book your call'; } catch (err) { showError('Something went wrong. Please try again or email us directly.'); } finally { setLoading(false); if (submitSucceeded) { const btnText = submitBtn.querySelector('.hf-btn-text'); if (btnText) { setTimeout(() => { btnText.textContent = originalBtnText; }, 3500); } } } }); function setLoading(state) { submitBtn.disabled = state; if (state) { submitBtn.classList.add('loading'); } else { submitBtn.classList.remove('loading'); } } function showError(msg) { errorBox.textContent = msg; errorBox.classList.add('visible'); } function hideError() { errorBox.textContent = ''; errorBox.classList.remove('visible'); } function isValidEmail(email) { return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email); } });