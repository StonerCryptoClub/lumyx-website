(function() { 'use strict'; const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches; const isTouchDevice = ('ontouchstart' in window) || (navigator.maxTouchPoints > 0); const CONFIG = { tiltAngleLimit: 8, tiltSmoothness: 0.1, glowUpdateThrottle: 16, scrollThreshold: 0.15, magneticStrength: 0.15, enableMagnetic: false }; function throttle(func, delay) { let timeoutId; let lastExec = 0; return function(...args) { const elapsed = Date.now() - lastExec; const execute = () => { lastExec = Date.now(); func.apply(this, args); }; if (elapsed > delay) { execute(); } else { clearTimeout(timeoutId); timeoutId = setTimeout(execute, delay - elapsed); } }; } function getCardBounds(element) { if (!element._cachedBounds || Date.now() - element._boundsTimestamp > 100) { element._cachedBounds = element.getBoundingClientRect(); element._boundsTimestamp = Date.now(); } return element._cachedBounds; } function getRelativePosition(event, element) { const bounds = getCardBounds(element); const centerX = bounds.left + bounds.width / 2; const centerY = bounds.top + bounds.height / 2; return { x: ((event.clientX - centerX) / bounds.width) * 2, y: ((event.clientY - centerY) / bounds.height) * 2, centerX, centerY }; } function initScrollAnimations() { if (prefersReducedMotion) { document.querySelectorAll('.service-card, .industry-card').forEach(card => { card.classList.add('animate-in'); }); return; } const observerOptions = { threshold: CONFIG.scrollThreshold, rootMargin: '0px 0px -10% 0px' }; const observer = new IntersectionObserver((entries) => { entries.forEach(entry => { if (entry.isIntersecting && !entry.target.classList.contains('animate-in')) { entry.target.classList.add('animate-in'); observer.unobserve(entry.target); } }); }, observerOptions); document.querySelectorAll('.service-card, .industry-card').forEach(card => { observer.observe(card); }); } function init3DTilt() { if (prefersReducedMotion || isTouchDevice) return; const cards = document.querySelectorAll('.service-card, .industry-card'); cards.forEach(card => { card.classList.add('tilt-enabled'); let tiltX = 0; let tiltY = 0; const handleMouseMove = throttle((e) => { const pos = getRelativePosition(e, card); const targetTiltX = -pos.y * CONFIG.tiltAngleLimit; const targetTiltY = pos.x * CONFIG.tiltAngleLimit; tiltX += (targetTiltX - tiltX) * CONFIG.tiltSmoothness; tiltY += (targetTiltY - tiltY) * CONFIG.tiltSmoothness; requestAnimationFrame(() => { card.style.setProperty('--tiltX', `${tiltY}deg`); card.style.setProperty('--tiltY', `${tiltX}deg`); }); }, CONFIG.glowUpdateThrottle); const handleMouseLeave = () => { requestAnimationFrame(() => { card.style.setProperty('--tiltX', '0deg'); card.style.setProperty('--tiltY', '0deg'); }); }; card.addEventListener('mousemove', handleMouseMove); card.addEventListener('mouseleave', handleMouseLeave); }); } function initDynamicGlow() { if (prefersReducedMotion || isTouchDevice) return; const cards = document.querySelectorAll('.service-card, .industry-card'); cards.forEach(card => { const handleMouseMove = throttle((e) => { const bounds = getCardBounds(card); const x = ((e.clientX - bounds.left) / bounds.width) * 100; const y = ((e.clientY - bounds.top) / bounds.height) * 100; requestAnimationFrame(() => { card.style.setProperty('--x', `${x}%`); card.style.setProperty('--y', `${y}%`); }); }, CONFIG.glowUpdateThrottle); card.addEventListener('mousemove', handleMouseMove); }); } function initIconAnimations() { const icons = document.querySelectorAll('.service-icon svg, .industry-card .emoji'); icons.forEach(icon => { if (icon.tagName === 'svg') { const paths = icon.querySelectorAll('path'); paths.forEach(path => { const length = path.getTotalLength(); path.style.strokeDasharray = length; path.style.strokeDashoffset = length; const observer = new IntersectionObserver((entries) => { entries.forEach(entry => { if (entry.isIntersecting) { path.style.transition = 'stroke-dashoffset 1s ease-out'; path.style.strokeDashoffset = '0'; observer.unobserve(entry.target); } }); }, { threshold: 0.5 }); observer.observe(icon); }); } }); } function initTouchInteractions() { if (!isTouchDevice) return; const cards = document.querySelectorAll('.service-card, .industry-card'); cards.forEach(card => { let touchActivated = false; card.addEventListener('touchstart', (e) => { if (!touchActivated) { e.preventDefault(); card.classList.add('touch-active'); touchActivated = true; const touch = e.touches[0]; const bounds = card.getBoundingClientRect(); const x = ((touch.clientX - bounds.left) / bounds.width) * 100; const y = ((touch.clientY - bounds.top) / bounds.height) * 100; card.style.setProperty('--x', `${x}%`); card.style.setProperty('--y', `${y}%`); setTimeout(() => { card.classList.remove('touch-active'); touchActivated = false; }, 300); } }, { passive: false }); card.addEventListener('click', (e) => { if (touchActivated) { touchActivated = false; } }); }); } function initMagneticEffect() { if (!CONFIG.enableMagnetic || prefersReducedMotion || isTouchDevice) return; const containers = document.querySelectorAll('.services-overview > .container'); containers.forEach(container => { const cards = container.querySelectorAll('.service-card, .industry-card'); const handleMouseMove = throttle((e) => { cards.forEach(card => { const bounds = getCardBounds(card); const centerX = bounds.left + bounds.width / 2; const centerY = bounds.top + bounds.height / 2; const distance = Math.hypot(e.clientX - centerX, e.clientY - centerY); const maxDistance = 200; if (distance < maxDistance) { const strength = (1 - distance / maxDistance) * CONFIG.magneticStrength; const deltaX = (e.clientX - centerX) * strength; const deltaY = (e.clientY - centerY) * strength; card.classList.add('magnetic-active'); card.style.setProperty('--magnetX', `${deltaX}px`); card.style.setProperty('--magnetY', `${deltaY}px`); } else { card.classList.remove('magnetic-active'); card.style.setProperty('--magnetX', '0px'); card.style.setProperty('--magnetY', '0px'); } }); }, CONFIG.glowUpdateThrottle); container.addEventListener('mousemove', handleMouseMove); container.addEventListener('mouseleave', () => { cards.forEach(card => { card.classList.remove('magnetic-active'); card.style.setProperty('--magnetX', '0px'); card.style.setProperty('--magnetY', '0px'); }); }); }); } function setupBoundsCache() { const cards = document.querySelectorAll('.service-card, .industry-card'); const invalidateCache = throttle(() => { cards.forEach(card => { delete card._cachedBounds; delete card._boundsTimestamp; }); }, 100); window.addEventListener('resize', invalidateCache); window.addEventListener('scroll', invalidateCache, { passive: true }); } function init() { const hasCards = document.querySelector('.service-card, .industry-card'); if (!hasCards) return; document.querySelectorAll('.services-overview').forEach(section => { section.classList.add('cards-loaded'); }); initScrollAnimations(); init3DTilt(); initDynamicGlow(); initIconAnimations(); initTouchInteractions(); initMagneticEffect(); setupBoundsCache(); if (window.location.hostname === 'localhost' || window.location.hostname === '127.0.0.1') { console.log('✨ Enhanced Cards System Initialized', { reducedMotion: prefersReducedMotion, touchDevice: isTouchDevice, cardsCount: document.querySelectorAll('.service-card, .industry-card').length }); } } if (document.readyState === 'loading') { document.addEventListener('DOMContentLoaded', init); } else { init(); } document.addEventListener('visibilitychange', () => { if (document.hidden) { document.querySelectorAll('.service-card, .industry-card').forEach(card => { card.style.transition = 'none'; }); } else { document.querySelectorAll('.service-card, .industry-card').forEach(card => { card.style.transition = ''; }); } }); })();