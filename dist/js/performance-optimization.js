class PerformanceMonitor { constructor() { this.initObservers(); this.optimizeImages(); this.optimizeInteractions(); this.scheduleNonCriticalTasks(); } initObservers() { if ('PerformanceObserver' in window) { try { const lcpObserver = new PerformanceObserver((list) => { for (const entry of list.getEntries()) { console.log('LCP:', entry.startTime); } }); lcpObserver.observe({ entryTypes: ['largest-contentful-paint'] }); const clsObserver = new PerformanceObserver((list) => { for (const entry of list.getEntries()) { if (!entry.hadRecentInput) { console.log('CLS:', entry.value); } } }); clsObserver.observe({ entryTypes: ['layout-shift'] }); const inpObserver = new PerformanceObserver((list) => { for (const entry of list.getEntries()) { console.log('INP:', entry.duration); } }); inpObserver.observe({ entryTypes: ['event'] }); } catch (e) { console.log('Performance monitoring not supported'); } } } optimizeImages() { if ('IntersectionObserver' in window) { const imageObserver = new IntersectionObserver((entries, observer) => { entries.forEach(entry => { if (entry.isIntersecting) { const img = entry.target; if (img.dataset.src) { img.src = img.dataset.src; img.classList.add('loaded'); } if (img.dataset.srcset) { img.srcset = img.dataset.srcset; } img.classList.remove('loading'); observer.unobserve(img); } }); }, { rootMargin: '50px 0px', threshold: 0.01 }); const lazyImages = document.querySelectorAll('img[data-src]'); lazyImages.forEach(img => { img.classList.add('loading'); imageObserver.observe(img); }); } } optimizeInteractions() { let scrollTimeout; const handleScroll = () => { if (scrollTimeout) return; scrollTimeout = setTimeout(() => { requestAnimationFrame(() => { this.updateVisibleElements(); }); scrollTimeout = null; }, 16); }; window.addEventListener('scroll', handleScroll, { passive: true }); window.addEventListener('resize', handleScroll, { passive: true }); } updateVisibleElements() { const elements = document.querySelectorAll('.fade-in:not(.visible)'); const windowHeight = window.innerHeight; elements.forEach(element => { const elementTop = element.getBoundingClientRect().top; if (elementTop < windowHeight * 0.8) { element.classList.add('visible'); } }); } scheduleNonCriticalTasks() { const scheduleTask = (task, priority = 'background') => { if ('scheduler' in window && 'postTask' in window.scheduler) { return window.scheduler.postTask(task, { priority }); } else if ('requestIdleCallback' in window) { return requestIdleCallback(task); } else { return setTimeout(task, 0); } }; scheduleTask(() => { document.body.classList.add('animations-ready'); }); scheduleTask(() => { this.preloadLikelyPages(); }); } preloadLikelyPages() { const likelyPages = ['/blog.html', '/case-study.html']; likelyPages.forEach(page => { const link = document.createElement('link'); link.rel = 'prefetch'; link.href = page; document.head.appendChild(link); }); } breakUpLongTask(task, chunkSize = 5) { return new Promise((resolve) => { let index = 0; const processChunk = () => { const end = Math.min(index + chunkSize, task.length); for (let i = index; i < end; i++) { if (typeof task === 'function') { task(i); } } index = end; if (index < task.length) { setTimeout(processChunk, 0); } else { resolve(); } }; processChunk(); }); } loadThirdPartyScript(src, priority = 'low') { return new Promise((resolve, reject) => { const script = document.createElement('script'); script.src = src; script.async = true; if (priority === 'low') { setTimeout(() => { document.head.appendChild(script); }, 100); } else { document.head.appendChild(script); } script.onload = resolve; script.onerror = reject; }); } } function addResourceHints() { const hints = [ { rel: 'dns-prefetch', href: ' { rel: 'dns-prefetch', href: ' { rel: 'dns-prefetch', href: ' { rel: 'preconnect', href: 'https: { rel: 'preconnect', href: 'https: ]; hints.forEach(hint => { const link = document.createElement('link'); link.rel = hint.rel; link.href = hint.href; if (hint.rel === 'preconnect') { link.crossOrigin = 'anonymous'; } document.head.appendChild(link); }); } document.addEventListener('DOMContentLoaded', () => { addResourceHints(); const monitor = new PerformanceMonitor(); if ('serviceWorker' in navigator) { window.addEventListener('load', () => { navigator.serviceWorker.register('/service-worker.js') .then(registration => { console.log('SW registered: ', registration); }) .catch(registrationError => { console.log('SW registration failed: ', registrationError); }); }); } }); window.PerformanceOptimizer = { monitor: null, init() { this.monitor = new PerformanceMonitor(); return this.monitor; }, preloadImage(src) { const img = new Image(); img.src = src; }, defer(fn, delay = 0) { setTimeout(fn, delay); } };