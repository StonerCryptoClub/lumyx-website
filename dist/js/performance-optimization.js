const perfData = { start: performance.now(), metrics: {} }; document.addEventListener('DOMContentLoaded', function() { hidePageLoader(); optimizeImages(); preventLayoutShift(); if (window.requestIdleCallback) { requestIdleCallback(() => { optimizeThirdPartyScripts(); preloadCriticalResources(); trackPerformance(); }); } else { setTimeout(() => { optimizeThirdPartyScripts(); preloadCriticalResources(); trackPerformance(); }, 1000); } }); function hidePageLoader() { const loader = document.querySelector('.loading-screen'); if (loader) { loader.style.display = 'none'; } } function optimizeImages() { const images = document.querySelectorAll('img'); images.forEach(img => { if (!img.getAttribute('loading') && !img.closest('.hero-section')) { img.setAttribute('loading', 'lazy'); } img.addEventListener('error', function() { console.warn('Image failed to load:', this.src); this.style.display = 'none'; }); if (!img.getAttribute('width') && !img.getAttribute('height')) { img.style.aspectRatio = 'auto'; } }); } function preventLayoutShift() { const portfolioContainer = document.querySelector('#portfolio-grid'); if (portfolioContainer && !portfolioContainer.hasAttribute('data-optimized')) { portfolioContainer.style.minHeight = '400px'; portfolioContainer.setAttribute('data-optimized', 'true'); } const caseStudiesContainer = document.querySelector('#case-studies-grid'); if (caseStudiesContainer && !caseStudiesContainer.hasAttribute('data-optimized')) { caseStudiesContainer.style.minHeight = '600px'; caseStudiesContainer.setAttribute('data-optimized', 'true'); } } function optimizeThirdPartyScripts() { let calendlyLoaded = false; function loadCalendly() { if (calendlyLoaded) return; calendlyLoaded = true; const script = document.createElement('script'); script.src = 'https: script.async = true; document.head.appendChild(script); } let scrollLoaded = false; window.addEventListener('scroll', () => { if (!scrollLoaded && window.scrollY > 500) { scrollLoaded = true; loadCalendly(); } }, { passive: true }); setTimeout(loadCalendly, 5000); } function preloadCriticalResources() { const criticalResources = [ { href: 'https: { href: 'https: ]; criticalResources.forEach(resource => { const link = document.createElement('link'); link.rel = 'preload'; link.href = resource.href; link.as = resource.as; link.crossOrigin = 'anonymous'; document.head.appendChild(link); }); } function trackPerformance() { try { if (typeof PerformanceObserver !== 'undefined') { new PerformanceObserver((list) => { const entries = list.getEntries(); const lcpEntry = entries[entries.length - 1]; perfData.metrics.lcp = lcpEntry.startTime; }).observe({ entryTypes: ['largest-contentful-paint'] }); new PerformanceObserver((list) => { const entries = list.getEntries(); entries.forEach(entry => { perfData.metrics.fid = entry.processingStart - entry.startTime; }); }).observe({ entryTypes: ['first-input'] }); } window.addEventListener('load', () => { perfData.metrics.loadTime = performance.now() - perfData.start; console.log('Page Performance:', perfData.metrics); }); } catch (error) { console.warn('Performance tracking error:', error); } } window.addEventListener('error', function(e) { console.warn('Script error caught:', e.error); e.preventDefault(); }); let scrollTimeout; window.addEventListener('scroll', function() { if (scrollTimeout) return; scrollTimeout = setTimeout(() => { scrollTimeout = null; }, 16); }, { passive: true }); if ('serviceWorker' in navigator) { window.addEventListener('load', () => { navigator.serviceWorker.register('/public/service-worker.js') .catch(() => { }); }); } console.log('✅ Performance optimizations loaded - preserving all functionality');