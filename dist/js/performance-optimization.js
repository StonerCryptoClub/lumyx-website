class AdvancedPerformanceOptimizer { constructor() { this.isInitialized = false; this.taskQueue = []; this.isProcessingTasks = false; this.loadedResources = new Set(); this.performanceMetrics = {}; this.init(); } init() { if (this.isInitialized) return; this.isInitialized = true; this.setupTaskScheduler(); this.initIntersectionObservers(); this.setupYieldBasedLoading(); this.initPerformanceMonitoring(); this.optimizeCriticalCSS(); this.initAdvancedImageOptimization(); this.initServiceWorker(); } setupTaskScheduler() { this.scheduleTask = (callback, priority = 'user-blocking') => { if ('scheduler' in window && 'postTask' in window.scheduler) { return window.scheduler.postTask(callback, { priority }); } return this.yieldToMain(callback); }; this.scheduleBackgroundTask = (callback) => { return this.scheduleTask(callback, 'background'); }; this.scheduleUserVisibleTask = (callback) => { return this.scheduleTask(callback, 'user-visible'); }; } async yieldToMain(callback) { return new Promise(resolve => { const channel = new MessageChannel(); channel.port2.onmessage = () => { try { const result = callback(); resolve(result); } catch (error) { console.error('Task execution error:', error); resolve(null); } }; channel.port1.postMessage(null); }); } async processTasksInChunks(tasks, chunkSize = 5) { const results = []; for (let i = 0; i < tasks.length; i += chunkSize) { const chunk = tasks.slice(i, i + chunkSize); const chunkResults = await Promise.all( chunk.map(task => this.scheduleBackgroundTask(task)) ); results.push(...chunkResults); if (i + chunkSize < tasks.length) { await this.yieldToMain(() => {}); } } return results; } initIntersectionObservers() { this.calendlyObserver = new IntersectionObserver((entries) => { entries.forEach(entry => { if (entry.isIntersecting) { this.loadCalendlyOptimized(); this.calendlyObserver.disconnect(); } }); }, { rootMargin: '500px', threshold: 0.1 }); this.lazyObserver = new IntersectionObserver((entries) => { entries.forEach(entry => { if (entry.isIntersecting) { this.scheduleBackgroundTask(() => { const element = entry.target; if (element.dataset.src) { element.src = element.dataset.src; element.removeAttribute('data-src'); } if (element.dataset.srcset) { element.srcset = element.dataset.srcset; element.removeAttribute('data-srcset'); } this.lazyObserver.unobserve(element); }); } }); }, { rootMargin: '50px' }); document.addEventListener('DOMContentLoaded', () => { const bookingSection = document.getElementById('booking-section'); if (bookingSection) { this.calendlyObserver.observe(bookingSection); } document.querySelectorAll('[data-src]').forEach(img => { this.lazyObserver.observe(img); }); }); } async loadCalendlyOptimized() { if (this.loadedResources.has('calendly')) return; this.loadedResources.add('calendly'); await this.scheduleBackgroundTask(() => { const script = document.createElement('script'); script.src = 'https: script.async = true; script.defer = true; script.onload = () => { this.scheduleBackgroundTask(() => this.initializeCalendlyWidget()); }; document.head.appendChild(script); }); } async initializeCalendlyWidget() { let attempts = 0; const maxAttempts = 30; const tryInit = async () => { if (typeof window.Calendly !== 'undefined') { await this.scheduleUserVisibleTask(() => { window.Calendly.initInlineWidget({ url: 'https: parentElement: document.getElementById('calendly-container'), prefill: {}, utm: {}, styles: { height: '650px' } }); }); } else if (attempts < maxAttempts) { attempts++; setTimeout(tryInit, 100); } }; tryInit(); } setupYieldBasedLoading() { const criticalScripts = [ 'js/env-loader.js', 'js/html-sanitizer.js', 'js/input-validator.js' ]; const nonCriticalScripts = [ 'https: 'https: 'https: 'https: ]; document.addEventListener('DOMContentLoaded', async () => { await this.loadScriptsWithYielding(criticalScripts, 'user-visible'); }); this.setupInteractionBasedLoading(nonCriticalScripts); } async loadScriptsWithYielding(scripts, priority = 'background') { const loadTasks = scripts.map(src => () => { return new Promise((resolve, reject) => { if (this.loadedResources.has(src)) { resolve(); return; } const script = document.createElement('script'); script.src = src; script.async = true; script.defer = true; script.onload = () => { this.loadedResources.add(src); resolve(); }; script.onerror = reject; document.head.appendChild(script); }); }); await this.processTasksInChunks(loadTasks, 2); } setupInteractionBasedLoading(scripts) { let scriptsLoaded = false; const loadScripts = async () => { if (scriptsLoaded) return; scriptsLoaded = true; await this.loadScriptsWithYielding(scripts, 'background'); }; const events = ['click', 'scroll', 'keydown', 'touchstart', 'mousemove', 'wheel']; events.forEach(event => { document.addEventListener(event, loadScripts, { once: true, passive: true, capture: true }); }); setTimeout(loadScripts, 1500); } initPerformanceMonitoring() { if (!('PerformanceObserver' in window)) return; const longTaskObserver = new PerformanceObserver((list) => { for (const entry of list.getEntries()) { if (entry.duration > 50) { this.performanceMetrics.longTasks = this.performanceMetrics.longTasks || []; this.performanceMetrics.longTasks.push({ duration: entry.duration, startTime: entry.startTime, name: entry.name }); if (entry.duration > 100) { console.warn(`Long task detected: ${entry.duration}ms - consider breaking this up`); } } } }); const vitalsObserver = new PerformanceObserver((list) => { for (const entry of list.getEntries()) { this.performanceMetrics[entry.name] = entry.value; } }); try { longTaskObserver.observe({ entryTypes: ['longtask'] }); vitalsObserver.observe({ entryTypes: ['navigation', 'paint', 'largest-contentful-paint'] }); } catch (e) { console.warn('Performance observer not fully supported'); } } optimizeCriticalCSS() { const criticalStyles = [ 'css/main.css', 'css/styles.css' ]; criticalStyles.forEach(href => { const link = document.createElement('link'); link.rel = 'preload'; link.as = 'style'; link.href = href; link.onload = function() { this.rel = 'stylesheet'; }; document.head.appendChild(link); }); } initAdvancedImageOptimization() { const supportsWebP = () => { const canvas = document.createElement('canvas'); canvas.width = 1; canvas.height = 1; return canvas.toDataURL('image/webp').indexOf('data:image/webp') === 0; }; if (supportsWebP()) { document.documentElement.classList.add('webp'); } document.addEventListener('DOMContentLoaded', () => { const images = document.querySelectorAll('img:not([loading])'); images.forEach(img => { img.loading = 'lazy'; img.decoding = 'async'; }); }); } initServiceWorker() { if ('serviceWorker' in navigator) { window.addEventListener('load', async () => { try { await navigator.serviceWorker.register('/service-worker.js'); } catch (error) { console.warn('Service Worker registration failed:', error); } }); } } getMetrics() { return this.performanceMetrics; } } if (typeof window !== 'undefined') { window.performanceOptimizer = new AdvancedPerformanceOptimizer(); } export default AdvancedPerformanceOptimizer;