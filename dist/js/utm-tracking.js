(function () { 'use strict'; var KEY = 'lumyx_attribution'; var FIELDS = [ 'utm_source', 'utm_medium', 'utm_campaign', 'utm_term', 'utm_content', 'gclid', 'fbclid', 'ttclid', 'msclkid' ]; function read() { try { return JSON.parse(localStorage.getItem(KEY) || 'null'); } catch (e) { return null; } } function write(obj) { try { localStorage.setItem(KEY, JSON.stringify(obj)); } catch (e) {} try { sessionStorage.setItem(KEY, JSON.stringify(obj)); } catch (e) {} } function deriveChannel(data, referrer) { var s = String(data.utm_source || '').toLowerCase(); var m = String(data.utm_medium || '').toLowerCase(); // Meta: fbclid click ID or any recognisable Meta source/medium if (data.fbclid || /facebook|fb|meta|instagram|ig/.test(s) || /paid.?social|cpc/.test(m) && /facebook|instagram|fb/.test(s)) return 'meta'; // Google: gclid click ID or Google Ads source strings if (data.gclid || /^google$|adwords|google.?ads|gads|pmax|google.?shopping/.test(s)) return 'google'; // TikTok if (data.ttclid || /tiktok|tik.?tok/.test(s)) return 'tiktok'; // Bing if (data.msclkid || /bing|microsoft/.test(s)) return 'bing'; // Any other explicit source — slug it if (s) return s.replace(/[^a-z0-9]+/g, '-').replace(/^-+|-+$/g, ''); // Fallback: infer from HTTP referrer var ref = String(referrer || '').toLowerCase(); if (/facebook\.com|instagram\.com|fb\.me/.test(ref)) return 'meta'; if (/google\.com/.test(ref)) return 'google'; if (/bing\.com/.test(ref)) return 'bing'; if (/tiktok\.com/.test(ref)) return 'tiktok'; return ref ? 'referral' : 'direct'; } var params = new URLSearchParams(window.location.search); var current = {}; FIELDS.forEach(function (k) { var v = params.get(k); if (v) current[k] = String(v).slice(0, 255); }); var stored = read(); // First-touch wins: only write attribution once per browser unless none captured yet. if (!stored || !stored.captured) { stored = { captured: true, channel: deriveChannel(current, document.referrer), landingPage: window.location.href.slice(0, 500), referrer: (document.referrer || '').slice(0, 500), capturedAt: new Date().toISOString() }; FIELDS.forEach(function (k) { stored[k] = current[k] || ''; }); write(stored); } window.LumyxAttribution = stored; window.getLumyxAttribution = function () { return window.LumyxAttribution || read() || {}; }; })();